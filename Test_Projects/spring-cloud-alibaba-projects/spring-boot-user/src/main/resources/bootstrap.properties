# 配置中心必须是使用bootstrap.properties。同时，spring.application.name值必须与上一阶段Nacos中创建的配置Data Id匹配（除了.properties或者.yaml后缀）。
spring.application.name=user
server.port=10001
spring.cloud.nacos.config.server-addr=192.168.50.181:8848